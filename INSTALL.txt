Installation instructions:
 * Unzip all files to a folder
 * Open index.html in supported web browser (Chrome, Firefox, Edge)

All necessary libraries are included locally in the lib folder.


To run unit tests, open tests.html in a browser. The results are printed to the console. The tests check various inputs into the filterCourses() function against expected results.